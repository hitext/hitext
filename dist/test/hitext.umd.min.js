/* global hitext */
import fs from 'fs';
import { textHitext } from './test.js';

describe('dist/hitext.umd.min.cjs', () => {
    before(() => new Function(fs.readFileSync('dist/hitext.umd.min.cjs'))());

    it('basic', () => {
        textHitext(hitext);
    });
});
